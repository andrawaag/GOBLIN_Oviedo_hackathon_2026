# Uncompressed rendered RDF — multi-GB, regenerable via scripts/sssom_serialize.py.
# The GZIPPED versions (*.trig.gz / *.ttls.gz) ARE committed via Git LFS — see .gitattributes.
sssom_all.trig
sssom_all.ttls
*.trig
*.ttls
*.sssom.tsv

# …but DO keep the small, self-contained browser-demo samples
!docs/sample.trig
!docs/sample.ttls

# logs / scratch
*.log
__pycache__/
.DS_Store
